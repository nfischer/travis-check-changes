{
  "name": "travis-check-changes",
  "version": "0.2.0",
  "description": "Make sure that no files were changed after travis finishes running",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "bin": {
    "travis-check-changes": "bin/travis-check-changes.js"
  },
  "keywords": [
    "travis",
    "CI",
    "changes",
    "after"
  ],
  "author": "Nate Fischer",
  "license": "MIT",
  "dependencies": {
    "shelljs": "^0.7.0"
  }
}
